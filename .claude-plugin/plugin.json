{
  "name": "harness",
  "version": "1.3.1",
  "description": "Decision → decomposition → issue execution harness. /dig (frontier + user, no build), /carve (planner: architecture manifest, feature→ticket→issue, independent challenge), /crank (control plane: claim, worktree, scope/import/ownership gates, verify, hook-signed review, merge, smoke, integrate, recovery, cleanup).",
  "author": {
    "name": "beastgummy0321-stack"
  }
}
